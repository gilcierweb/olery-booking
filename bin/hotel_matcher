#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/olery_booking/input_helper.rb"
require_relative "../lib/olery_booking/request.rb"

puts hotel = ARGV[0]

OleryBooking::Request.new(hotel).run()